import React, { Component } from "react";

export default class Logout extends Component {
  render() {
    return (
      <span className="auth-signin">
        <p className=" btn btn-warning auth__btn btn-signin">Logout</p>
      </span>
    );
  }
}
